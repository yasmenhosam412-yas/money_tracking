/// Internal key for balance UI and localization; must match
/// [localizeIncomeCategory] `__unassigned__` case in `l10n_entity_strings.dart`.
abstract final class BalanceSourceConstants {
  BalanceSourceConstants._();

  static const unassigned = '__unassigned__';
}
